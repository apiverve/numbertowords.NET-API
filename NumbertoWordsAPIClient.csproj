<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.NumbertoWords</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/numbertowords</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Number to Words is a simple tool for converting numbers to words. It returns the number in words. It can also do the reverse, and convert words to numbers</Description>
    <RepositoryUrl>https://github.com/apiverve/numbertowords-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>number to words; number conversion; number to words api; number conversion api; number to words tool; number conversion tool; number to words software; number conversion software; number to words service; number conversion service; number to words library; number conversion library; number to words sdk; number conversion sdk; number to words code; number conversion code; number to words script; number conversion script; number to words program; number conversion program; number to words application; number conversion application; number to words app; number conversion app; number to words utility; number conversion utility; number to words helper; number conversion helper; number to words helper tool; number conversion helper tool; number to words helper software; number conversion helper software; number to words helper service; number conversion helper service; number to words helper library; number conversion helper library; number to words helper sdk; number conversion helper sdk; number to words helper code; number conversion helper code; number to words helper script; number conversion helper script; number to words helper program; number conversion helper program; number to words helper application; number conversion helper application; number to words helper app; number conversion helper app; number to words helper tool; number conversion helper tool; number to words helper utility; number conversion helper utility; number to words helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
